
namespace example { 
	u64 alloc_texture(u64 width, u64 height, u64 page_size);
};

// You can safely delete this example interface definition
interface Greeter {
	constructor(string name);
	string greet();
};